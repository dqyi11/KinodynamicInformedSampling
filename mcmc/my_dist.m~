%% Function for euclidean distance
function [d] = my_dist(x)
% Distance function 
    global x0;
%     x = reshape(x, [size(x,1)*size(x,1), size(x,2)]);
%     no_ele = size(x,1);
    d = sqrt(sum((x - repmat(x0, siz)).^2,2));
end